--single comment

/*
multiple comments
*/

CREATE DATABASE batch198; --SQL in not case sensitive, to make it more READABLE, we use capitals
DROP DATABASE batch198;

-- Create a table using SQL queries

CREATE TABLE student (
	id CHAR(4), 
	name VARCHAR(30),
	age INT,
 DoB DATE
	);
	SELECT * FROM 
CREATE TABLE students1 AS SELECT id, name FROM students;
	DROP TABLE students1;
	
	
	
	
	